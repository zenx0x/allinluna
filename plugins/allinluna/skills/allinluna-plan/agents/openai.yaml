interface:
  display_name: "All in Luna — Plan"
  short_description: "Plan complete top-level owner workflows"
  default_prompt: "Use $allinluna-plan to produce a complete plan whose independent owner lanes are user-visible top-level Codex tasks; allow each owner bounded internal subagents and do not create a Goal."
