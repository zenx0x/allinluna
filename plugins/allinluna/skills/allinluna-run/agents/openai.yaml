interface:
  display_name: "All in Luna — 执行"
  short_description: "通过多个侧边栏顶层任务并行实施、集成并验收完整计划"
  default_prompt: "使用 $allinluna-run 完整执行计划；确保 top_level_tasks=true 并派发侧边栏可见的顶层 Codex 任务，非 Git 项目先请求 Git 准备授权，负责人可使用有界 subagents，且不创建 Goal。"
