"""End-to-end contracts for the All in Luna vNext runtime.

The tests in this package are intentionally runtime-facing.  They do not
replace a missing vNext runtime with a local simulator; the scenario tests
skip with an explicit blocked reason until the runtime test hook exists.
"""
