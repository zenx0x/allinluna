interface:
  display_name: "All in Luna — Run"
  short_description: "Run through top-level Codex task owners"
  default_prompt: "Use $allinluna-run to execute completely by dispatching independent owner lanes as user-visible top-level Codex tasks; each owner may use bounded internal subagents, but the root must not replace owner tasks with subagents and must not create a Goal."
