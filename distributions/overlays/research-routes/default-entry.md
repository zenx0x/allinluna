# Research Routes entry

Start with `$research-routes` to build a route-neutral terrain map, separate Claims from Evidence, compare competing approaches, record failure regimes and unknowns, and choose a reversible next probe. A route is not an implementation order, experiment authorization, HumanDecision, or canonical-state promotion. When a route is ready for delivery, hand it to All in Luna for development concurrency.
