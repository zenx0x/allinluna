# All in Luna entry

Start with `$allinluna-plan` when you need a complete, dependency-aware development plan. Use `$allinluna-run` after approval, or use `parallel-only` when the plan already exists. All in Luna is the development-concurrency distribution: it coordinates owners, worktrees, recovery, integration, and acceptance without creating a Goal unless the user explicitly asks.
