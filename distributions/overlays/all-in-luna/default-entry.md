# All in Luna entry

Start with the single `$allinluna` Skill. It compiles a goal, existing plan, active run, or Research Routes packet into the canonical runtime, then executes through Coordinator, Task Lanes, WorkUnits, Packs, and receipts. Legacy inputs are read-only imports; push, publication, deployment, and live external mutation are requested just in time.
