# Example only. Replace placeholders with routes available in your host/deployment.
# All in Luna Core does not provide or require these model names.

model_policy: auto
reasoning_policy: auto
route_assurance: observe_if_exposed
external_action_policy: ask

capability_routes:
  planning.semantic:
    model: "${STRONG_MODEL}"
    reasoning: "${HIGH_REASONING}"
  lane.synthesis:
    model: "${STRONG_MODEL}"
    reasoning: "${HIGH_REASONING}"
  work.deep-debug:
    model: "${STRONG_MODEL}"
    reasoning: "${HIGH_REASONING}"
  verify.independent:
    model: "${VERIFY_MODEL}"
    reasoning: "${HIGH_REASONING}"
