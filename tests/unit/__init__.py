"""vNext unit and property contract tests.

These tests intentionally never import the legacy runtime.  The vNext package
is required, and missing modules or protocol symbols fail rather than skip.
"""
