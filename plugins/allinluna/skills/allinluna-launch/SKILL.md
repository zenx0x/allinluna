---
name: allinluna-launch
description: Prepare the single concise launch confirmation before formal All in Luna execution, capturing plan source and adjustment permission, work type, profile, model, reasoning, concurrency, Coordinator and CounterPilot, Goal, Git/worktree/commit/push, external mutation, toolchain, budget, and one-time 16-plus high-capability decomposition choice. Use after conversation intake and before starting a run.
---

# All in Luna - Launch Confirmation

Create exactly one pending launch record, show its concise confirmation prompt, and formalize
execution only after confirmation. Include source/adjustment permission, work type, resource profile,
requested model and reasoning, concurrency, separate Coordinator, CounterPilot, Goal authorization,
Git/worktree/commit/push boundaries, external mutation policy, toolchain, budget, and the one-time
decomposition choice when concurrency is 16 or higher.

External-plan-complete intake is parallel-only: normalize dependencies, ownership, resources,
permissions, stop boundary, and recovery metadata, but do not redesign product direction.

Use `scripts/launch.py` for deterministic records. A pending record is not execution and must not
create a Goal or mutate an external service.
